---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: cpgingesters.apps.cpgtoacp.io
spec:
  group: apps.cpgtoacp.io
  names:
    kind: CPGIngester
    listKind: CPGIngesterList
    plural: cpgingesters
    singular: cpgingester
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CPGIngester is the Schema for the cpgingesters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CPGIngesterSpec defines the desired state of CPGIngester.
            properties:
              artifactStore:
                description: ArtifactStore configures shared artifact storage.
                properties:
                  artifactBucket:
                    default: cpg-artifacts
                    description: ArtifactBucket contains non-PHI pipeline artifacts.
                    type: string
                  credentialsProvider:
                    description: |-
                      CredentialsProvider is an OpenShell provider that supplies
                      ARTIFACT_STORE_ACCESS_KEY and ARTIFACT_STORE_SECRET_KEY.
                    type: string
                  phiBucket:
                    description: PHIBucket contains artifacts with protected health
                      information.
                    type: string
                  url:
                    description: URL is the artifact store endpoint.
                    type: string
                type: object
              assembly:
                description: Assembly assembles generated artifacts into the published
                  bundle.
                properties:
                  extraEnv:
                    additionalProperties:
                      type: string
                    description: ExtraEnv contains non-secret environment variables
                      that do not yet have first-class fields.
                    type: object
                  extraProviders:
                    description: |-
                      ExtraProviders attaches additional OpenShell credential providers to this component.
                      Domain-specific providers should be configured on their corresponding integration.
                    items:
                      type: string
                    type: array
                  image:
                    description: Image is the complete container image reference,
                      including its registry and tag or digest.
                    minLength: 1
                    type: string
                  pythonPath:
                    description: PythonPath is translated to PYTHONPATH.
                    type: string
                required:
                - image
                type: object
              bff:
                description: BFF exposes the backend API used by the CPG Ingester
                  UI.
                properties:
                  extraEnv:
                    additionalProperties:
                      type: string
                    description: ExtraEnv contains non-secret environment variables
                      that do not yet have first-class fields.
                    type: object
                  extraProviders:
                    description: |-
                      ExtraProviders attaches additional OpenShell credential providers to this component.
                      Domain-specific providers should be configured on their corresponding integration.
                    items:
                      type: string
                    type: array
                  image:
                    description: Image is the complete container image reference,
                      including its registry and tag or digest.
                    minLength: 1
                    type: string
                  pythonPath:
                    description: PythonPath is translated to PYTHONPATH.
                    type: string
                required:
                - image
                type: object
              carePlanWriterRef:
                description: |-
                  CarePlanWriterRef selects the CarePlanWriter integrated with this pipeline.
                  The operator resolves its generated backend address.
                properties:
                  name:
                    description: Name is the metadata.name of the referenced pipeline
                      resource.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              delivery:
                description: Delivery publishes assembled artifacts to the artifact
                  store.
                properties:
                  extraEnv:
                    additionalProperties:
                      type: string
                    description: ExtraEnv contains non-secret environment variables
                      that do not yet have first-class fields.
                    type: object
                  extraProviders:
                    description: |-
                      ExtraProviders attaches additional OpenShell credential providers to this component.
                      Domain-specific providers should be configured on their corresponding integration.
                    items:
                      type: string
                    type: array
                  image:
                    description: Image is the complete container image reference,
                      including its registry and tag or digest.
                    minLength: 1
                    type: string
                  pythonPath:
                    description: PythonPath is translated to PYTHONPATH.
                    type: string
                required:
                - image
                type: object
              ingestion:
                description: Ingestion parses source CPG documents and performs OCR
                  when required.
                properties:
                  doclingArtifactsPath:
                    description: DoclingArtifactsPath contains preloaded Docling models.
                    type: string
                  doclingCacheDirectory:
                    description: DoclingCacheDirectory stores downloaded Docling data.
                    type: string
                  doclingLogLevel:
                    description: DoclingLogLevel controls Docling library logging.
                    type: string
                  extraEnv:
                    additionalProperties:
                      type: string
                    description: ExtraEnv contains non-secret environment variables
                      that do not yet have first-class fields.
                    type: object
                  extraProviders:
                    description: |-
                      ExtraProviders attaches additional OpenShell credential providers to this component.
                      Domain-specific providers should be configured on their corresponding integration.
                    items:
                      type: string
                    type: array
                  huggingFaceOffline:
                    default: true
                    description: HuggingFaceOffline prevents model downloads at runtime.
                    type: boolean
                  huggingFaceTransferEnabled:
                    description: HuggingFaceTransferEnabled enables the Hugging Face
                      accelerated transfer client.
                    type: boolean
                  image:
                    description: Image is the complete container image reference,
                      including its registry and tag or digest.
                    minLength: 1
                    type: string
                  logLevel:
                    description: LogLevel controls application logging.
                    type: string
                  ocrEnabled:
                    default: true
                    description: OCREnabled enables the conditional OCR pass for scanned
                      documents.
                    type: boolean
                  pythonUnbuffered:
                    default: true
                    description: PythonUnbuffered enables unbuffered Python output.
                    type: boolean
                required:
                - image
                type: object
              llm:
                description: LLM configures the model endpoint used by LLM analysis.
                properties:
                  credentialsProvider:
                    description: CredentialsProvider is an OpenShell provider that
                      supplies LLM_API_KEY.
                    type: string
                  model:
                    default: default
                    description: Model is the model identifier sent to the endpoint.
                    type: string
                  requestTimeoutSeconds:
                    default: 600
                    description: RequestTimeoutSeconds is the maximum duration of
                      an LLM request.
                    format: int32
                    minimum: 1
                    type: integer
                  url:
                    description: URL is the LiteLLM-compatible endpoint.
                    type: string
                type: object
              llmAnalysis:
                description: LLMAnalysis extracts decision logic and recommendations
                  with an LLM.
                properties:
                  extraEnv:
                    additionalProperties:
                      type: string
                    description: ExtraEnv contains non-secret environment variables
                      that do not yet have first-class fields.
                    type: object
                  extraProviders:
                    description: |-
                      ExtraProviders attaches additional OpenShell credential providers to this component.
                      Domain-specific providers should be configured on their corresponding integration.
                    items:
                      type: string
                    type: array
                  figureInterpretationEnabled:
                    default: true
                    description: FigureInterpretationEnabled enables vision analysis
                      of document figures.
                    type: boolean
                  figureInterpretationMaxFigures:
                    default: 100
                    description: FigureInterpretationMaxFigures limits vision calls
                      per document.
                    format: int32
                    minimum: 0
                    type: integer
                  image:
                    description: Image is the complete container image reference,
                      including its registry and tag or digest.
                    minLength: 1
                    type: string
                  pythonPath:
                    description: PythonPath is translated to PYTHONPATH.
                    type: string
                required:
                - image
                type: object
              observability:
                description: Observability configures shared tracing.
                properties:
                  mlflowTrackingURI:
                    description: MLflowTrackingURI is translated to MLFLOW_TRACKING_URI.
                    type: string
                type: object
              ui:
                description: UI serves the CPG Ingester web application.
                properties:
                  extraEnv:
                    additionalProperties:
                      type: string
                    description: ExtraEnv contains non-secret environment variables
                      that do not yet have first-class fields.
                    type: object
                  extraProviders:
                    description: |-
                      ExtraProviders attaches additional OpenShell credential providers to this component.
                      Domain-specific providers should be configured on their corresponding integration.
                    items:
                      type: string
                    type: array
                  image:
                    description: Image is the complete container image reference,
                      including its registry and tag or digest.
                    minLength: 1
                    type: string
                required:
                - image
                type: object
            required:
            - assembly
            - bff
            - delivery
            - ingestion
            - llmAnalysis
            - ui
            type: object
          status:
            description: CPGIngesterStatus defines the observed state of CPGIngester.
            properties:
              conditions:
                description: Conditions summarize the current availability and reconciliation
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoints:
                additionalProperties:
                  type: string
                description: |-
                  Endpoints contains operator-published addresses for pipeline entry points.
                  Internal component wiring is intentionally not exposed as desired state.
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation reconciled
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
